
DEV_COMPOSE_YML=${DEV_COMPOSE_YML}' -f docker-compose-composer.yml';
DEV_SERVICE=composer;
DEV_USER=${UID};

if [ -n "${DEV_DATADIR}" ]; then
    DEV_SERVICE=' -w /app'${DEV_DATADIR}' '${DEV_SERVICE};
fi

DEV_CMD="${DEV_COMMAND_OPTIONS}";

