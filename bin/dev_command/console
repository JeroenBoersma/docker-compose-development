
dc service ${DEV_PHP};
dc opt "-u $(id -u):$(id -g)"
dc opt "--no-deps";

if [ -n "${DEV_PROJECTDIR}" ]; then
    dc opt "-w /data/${DEV_PROJECTDIR}"
fi

if [ -z "$*" ]; then
    # Interactive mode
    dc cmd bash -l;
else
    dc opt '-T';
    dc cmd "$*";
fi
