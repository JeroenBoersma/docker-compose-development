version: '3'

services:
  node:
    build: build/dist/node
    networks:
      - databases
    links:
      - db
      - mailcatch
      - redis
    volumes:
      - dockerdev-workspace-volume:/data

