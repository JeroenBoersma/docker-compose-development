version: '2'

services:
  php70:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro
  php56:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro
  php71:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro
  php72:
    environment:
      SSH_AUTH_SOCK: "/ssh-socket"
    volumes:
      - ${SSH_AUTH_SOCK}:/ssh-socket:ro

