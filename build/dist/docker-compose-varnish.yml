version: '3'

services:
  varnish:
    image: million12/varnish
    networks:
      - frontend
    links:
      - "web:web"
      - "web:backend"
    environment:
      - CACHE_SIZE
    volumes:
      - ./conf/varnish.vcl:/etc/varnish/default.vcl
  ssl:
    links:
      - "varnish:backend"

