services:
  varnish:
    image: varnish
    environment:
      - CACHE_SIZE
    volumes:
      - ./conf/varnish.vcl:/etc/varnish/default.vcl
  ssl:
    depends_on:
      - varnish
    links:
      - "varnish:backend"

