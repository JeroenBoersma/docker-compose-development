upstream fastcgi_backend {
    server fpm:9000;
}
upstream fastcgi56_backend {
    server fpm56:9000;
}
upstream fastcgi70_backend {
    server fpm70:9000;
}
upstream fastcgi71_backend {
    server fpm71:9000;
}
upstream fastcgi72_backend {
    server fpm72:9000;
}
upstream fastcgi73_backend {
    server fpm73:9000;
}
upstream fastcgi74_backend {
    server fpm74:9000;
}
upstream fastcgi80_backend {
    server fpm80:9000;
}
upstream fastcgi81_backend {
    server fpm81:9000;
}
upstream fastcgi82_backend {
    server fpm82:9000;
}
