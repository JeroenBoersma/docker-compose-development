version: '2'

services:
  php70:
    volumes:
    - ./workspace:/data
  php56:
    volumes:
    - ./workspace:/data
  php71:
    volumes:
    - ./workspace:/data
  php72:
    volumes:
    - ./workspace:/data
  web:
    volumes:
    - ./workspace:/data:ro
